String STRbasicSalary = textbox1.getText();
            // String STRta = textbox2.getText();
            // String STRda = textbox3.getText();
            // String STRhra = textbox4.getText();

            // double basicSalary = Integer.parseInt(STRbasicSalary), hra =
            // Integer.parseInt(STRhra),
            // ta = Integer.parseInt(STRta), da = Integer.parseInt(STRda), netSalary;
            // // calculating TA, DA and HRA

            // double TA, DA, HRA;

            // TA = basicSalary * ta;
            // DA = basicSalary * da;
            // HRA = basicSalary * hra;
            // // Calculating net salary by adding basicSalary, hra, da and deducting PF
            // from
            // // that
            // netSalary = basicSalary + hra + da + ta;

            // System.out.println("The Basic salary is:" + basicSalary);
            // System.out.println("The HRA is:" + hra);
            // System.out.println("The DA is:" + da);
            // System.out.println("The net salary is:" + netSalary);